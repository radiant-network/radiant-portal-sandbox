apiVersion: v1
kind: Service
metadata:
  name: radiant-polaris-mgmt
  labels:
    app: radiant-polaris-mgmt
spec:
  type: LoadBalancer
  selector:
    app: radiant-polaris
  ports:
    - port: 8182
      targetPort: 8182
      protocol: TCP
