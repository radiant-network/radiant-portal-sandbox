apiVersion: v1
data:
  init.sql: |
    CREATE DATABASE airflow WITH OWNER postgres;
    CREATE DATABASE radiant WITH OWNER postgres;
    CREATE DATABASE polaris WITH OWNER postgres;
    CREATE DATABASE keycloak WITH OWNER postgres;
kind: ConfigMap
metadata:
  name: postgres-init-scripts
