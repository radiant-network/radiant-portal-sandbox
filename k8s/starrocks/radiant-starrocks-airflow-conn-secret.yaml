apiVersion: v1
kind: Secret
metadata:
  labels:
    app: radiant-starrocks-fe
  name: starrocks-airflow-conn
type: Opaque
stringData:
  # Read by the dbt container (data_integrity_starrocks DAG) via secretKeyRef.
  # Must be JSON as the dbt container will parse it with json.loads
  AIRFLOW_CONN_STARROCKS_CONN: '{"host":"radiant-starrocks-fe","port":9030,"login":"root","password":"","schema":"radiant"}'