apiVersion: v1
kind: Secret
metadata:
  labels:
    app: radiant-starrocks-fe
  name: starrocks-airflow-conn
type: Opaque
stringData:
  # Lu par le conteneur dbt (DAG data_integrity_starrocks) via secretKeyRef.
  # Forme JSON car entrypoint.py fait json.loads dessus (host/port/login/password/schema).
  AIRFLOW_CONN_STARROCKS_CONN: '{"host":"radiant-starrocks-fe","port":9030,"login":"root","password":"","schema":"radiant"}'